{% extends 'InventaDespesasBundle::single.html.twig' %}

{% block singlecontent %}
    <h1>Fornecedor nº.{{entity.id}}</h1>
    <div class="col-md-8">
        <p><button type="button" class="btn btn-primary" onclick="goBack()">Voltar</button></p>
            
        {% if is_granted('ROLE_ADMIN') %}
            <p><a href="{{ path('suppliers_edit', { 'id': entity.id }) }}"><button class="btn btn-primary">Editar</button></a>
                {{ form(delete_form) }}</p>
            {% endif %}
        <table class="table table-striped">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Myinventa_id</th>
                    <td>{{ entity.myinventaid }}</td>
                </tr>
                <tr>
                    <th>Status</th>
                    <td>{{ entity.status }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ entity.name }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ entity.email }}</td>
                </tr>
                <tr>
                    <th>Phone</th>
                    <td>{{ entity.phone }}</td>
                </tr>
                <tr>
                    <th>Notes</th>
                    <td>{{ entity.notes }}</td>
                </tr>
                <tr>
                    <th>Date_added</th>
                    <td>{{ entity.dateadded|date('Y-m-d H:i:s') }}</td>
                </tr>
                <tr>
                    <th>Author_id</th>
                    <td>{{ entity.authorid }}</td>
                </tr>
            </tbody>
        </table>
    </div>

    <div class="col-md-6">
        <h2>Despesas</h2>
        {{ include('::tableexpenses.html.twig',{ 'entities': entity.expenses }) }}
        {{ include('::tabletransactionaplied.html.twig',{'entities':tra_a }) }}

    </div> 


{% endblock %}
